{
  "name": "fable-legacy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "pages:dev": "npm run build && wrangler pages dev dist --d1 FABLE_LEGACY_DB=fable-legacy-db --compatibility-date=2026-06-13 --persist-to=.wrangler/state",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=fable-legacy"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "wrangler": "^4.57.1"
  }
}
